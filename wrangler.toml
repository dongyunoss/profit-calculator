# Cloudflare 배포 설정
#
# Git 연동 배포가 classic Pages가 아니라 "정적 자산이 딸린 Worker" 방식(wrangler deploy)으로
# 처리되므로, functions/ 파일 기반 라우팅 대신 src/worker.js가 직접 /api/state를 연결하고
# 나머지 경로는 정적 자산(assets)으로 넘긴다. 빌드 도구는 없다 — 저장소 루트를 그대로 올린다.
#
# database_id 는 아래 명령으로 D1을 만들면 출력된다. 그 값을 여기에 붙여 넣는다.
#   wrangler d1 create profit-calculator

name = "profit-calculator"
compatibility_date = "2026-08-01"
main = "src/worker.js"

[assets]
directory = "."
binding = "ASSETS"

[[d1_databases]]
binding = "DB"                    # functions에서 env.DB 로 접근
database_name = "profit-calculator"
database_id = "ca15c6ae-8126-4203-8f9b-f7e34e26a770"

# SITE_KEY는 비밀값이라 저장소에 넣지 않고 Pages/Workers 대시보드에 "Secret"으로 등록한다.
#   Workers & Pages → 프로젝트 → Settings → Variables and Secrets
#     SITE_KEY = <임의의 긴 문자열>  (예: openssl rand -hex 24)
# 접속은 로그인 화면 없이 링크에 이 코드를 붙이는 방식이다.
#   https://<프로젝트>.pages.dev/?key=<위에서 정한 SITE_KEY>
